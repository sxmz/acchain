angular.module('acchain').service('checkloginservice', function($http, $rootScope,$location,$window,ipCookie) {
    // $rootScope.isLogin = false;
    // if(!$rootScope.isLogin){
    //     $window.location.href = '#/login'
    // }
});
